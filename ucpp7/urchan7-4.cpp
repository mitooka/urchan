#include <iostream>
#include <cstdint>//<cstdint>ヘッダでは、ビット数が規定された整数型の別名、およびマクロを提供する。これらの機能は、std名前空間に属することを除いてC言語の標準ライブラリ<stdint.h>ヘッダと同じである。
int main(){
    10 //int型
    10u//unsigmed log型
    10ul //unsigned log型
    INT16_c(10);//	8ビット幅を持つ、符号付き整数型の定数値
    std::cout << i << std::endl;

}