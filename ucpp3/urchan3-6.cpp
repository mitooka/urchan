#include <algorithm>
#include <iostream>
#include <vector>

int main(){//maxについて
    int a,b,c;
    std::cin >> a >> b >> c;
    std::cout << std::max({a,b,c}) << std::endl;
    


    // if(cond) 文 else 文
    // if(cond) 文 else if () 文
    // 文: exp;
    // 文 :{exp ; exp;}
    // if(a < b) min = a; else {min }

}